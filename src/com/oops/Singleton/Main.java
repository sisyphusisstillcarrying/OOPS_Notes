package com.oops.Singleton;

public class Main {
    public static void main(String[] args){
//        Singleton obj1 = new Singleton.getInstance();
//        // this is non static getInstance(); so to acces this we will have it as static whereever it is declared as we are accessing it via class name
//
//        Singleton obj2 = new Singleton.getInstance();
//        Singleton obj3 = new Singleton.getInstance();

        // all three variables are pointing to just the one object.
        // as these are not being able to call the constructor so they cannot create an object.

    }
}
